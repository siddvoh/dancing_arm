__pycache__/
*.pyc
*.pyo
.venv/
venv/
.env
.DS_Store
*.wav
*.mp3
*.flac
*.m4a
*.ogg
!samples/*.mp3
!samples/*.wav
!samples/*.flac

# runs/ is intentionally tracked so logs and recorded videos can be pushed
# back for analysis. (Silent fallback files are kept too so we can see the
# raw webcam capture when ffmpeg mux didn't run.)

.idea/
.vscode/
